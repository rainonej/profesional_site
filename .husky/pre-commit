#!/bin/sh

# Lint YAML files — mirrors the CI yamllint step exactly.
if ! command -v yamllint >/dev/null 2>&1; then
  echo "Error: yamllint is required. Install it with: pip install yamllint"
  exit 1
fi
yamllint .pages.yml .github/workflows/ || exit 1

cd site && npx lint-staged
